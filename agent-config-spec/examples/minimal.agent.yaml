# Example: Minimal config for quick start
# Works with any adapter — only required fields

agent:
  name: hello-agent
  description: Simple greeting agent

models:
  primary:
    model: gpt-4o-mini
    provider: openai
    settings:
      temperature: 0.7
      maxTokens: 2048
