{
  "name": "sayay-guard-interceptor",
  "version": "1.0.0",
  "description": "Lambda blueprint: financial interceptor — reads budget from DynamoDB, throws TokenBudgetExceededException when over budget (Tokenops-002)",
  "private": true,
  "type": "module",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@carloscortezcloud/sayay-guard": "^0.2.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
