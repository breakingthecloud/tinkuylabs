{
  "name": "styrr-llm-router",
  "version": "1.0.0",
  "description": "Lambda blueprint: multi-model load balancer — routes by complexity to economy (Llama 3.1 8B) vs reasoning (Claude 3 Sonnet) tiers (Tokenops-002)",
  "private": true,
  "type": "module",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.700.0",
    "@carloscortezcloud/styrr-llm": "^0.4.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
