{
  "name": "tinkuy-neptune-validator",
  "version": "1.0.0",
  "description": "Lambda blueprint: ontology verification layer — validates LLM output against the Tinkuy ontology module, aborts on structural hallucination, compresses valid payloads (Tokenops-002)",
  "private": true,
  "type": "module",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@carloscortezcloud/tinkuy-agent": "^0.5.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
